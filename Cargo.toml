[package]
name = "minecraft-font"
version = "0.1.0"
edition = "2024"

[features]
default = ["bitmaps"]
bitmaps = []

[build-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
image = { version = "0.25", default-features = false, features = ["png"] }
zip = "8"
